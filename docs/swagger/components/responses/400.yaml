description: Bad Request error message
content:
  application/json:
    schema:
      type: object
      # required:
      #   - status
      #   - message
      properties:
        status:
          type: integer
          example: 400
        message:
          type: array
          items:
            type: string
          example: ['"email" must be a valid email', '"name" is required']
